package main.java.standard;

import org.junit.jupiter.api.Test;

class PointComparatorTest {

    @Test
    public void test() {
        //PointArray then = new PointArray();
        //then.addAll(new int[] {1, 1, 2, 2}, new int[]{1, 2, 1, 2});

        //PointArray now = new PointArray();
        //now.addAll(new int[] {1, 2, 3, 4}, new int[]{1, 1, 1, 1});

        //PointComparator.Result result = new PointComparator(then, now).getResult();

        //PointArray created = new PointArray();
        //created.addAll(new int[] {3, 4}, new int[] {1, 1});
        //Assert.assertEquals(result.getCreated().toString(), result.getCreated(), created);

        //PointArray deleted = new PointArray();
        //deleted.addAll(new int[] {1, 2}, new int[] {2, 2});
        //Assert.assertEquals(result.getDeleted().toString(), result.getDeleted(), deleted);

        //PointArray retained = new PointArray();
        //retained.addAll(new int[] {1, 2}, new int[] {1, 1});
        //Assert.assertEquals(result.getRetained().toString(), result.getRetained(), retained);
    }

}
